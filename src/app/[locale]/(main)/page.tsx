/**
 * Home Page — Pure Orchestrator
 *
 * @remarks
 * Responsibilities (ONLY):
 * - Render HomePage feature root
 *
 * NO business logic, NO state management, NO direct service calls.
 */

"use client";

import { HomePage } from "@/features/home";

export default function Home() {
    return <HomePage />;
}
