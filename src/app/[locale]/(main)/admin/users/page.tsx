import { AdminUsersPageContent } from "@/features/admin";

/**
 * Admin Users Management Page.
 *
 * @remarks Direct UI orchestrator for searching, filtering, and managing platform users.
 * @example
 * <AdminUsersPage />
 */
export default function AdminUsersPage() {
    return <AdminUsersPageContent />;
}
