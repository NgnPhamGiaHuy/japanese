/**
 * Study Feature — Public API
 *
 * @remarks
 * Single entry point for Study session feature.
 * Enforces encapsulation — only exports what's needed externally.
 */

export { StudySession } from "./components";
