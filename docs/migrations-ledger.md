# Migrations ledger

**Location:** `docs/migrations-ledger.md` (this file). **Authority:** [ADR-120](adr/1xx-refactor-decisions.md) — _every staged change records its completion state_.

## Why this file exists

A **staged change** is one that lands in more than one step: a migration, a deprecation, or a capability shipped before its consumers. The repository has historically staged such work well and then lost track of it — six of the twelve root causes in the architecture assessment reduce to the same meta-cause, _"a migration or capability was staged with a defined later step, and the repository has no mechanism that records whether the later step happened or is still intended."_

The failure is not that stages exist. It is that a half-finished stage and a deliberately-final state look identical in the code. A reader — including the original author after time away — cannot tell whether a transitional shape is still intended.

Comments do not solve this: they do not expire, carry no owner, and cannot be reviewed on a date. A `@deprecated` marker says _what_ replaced something, never _whether anyone still intends to finish the replacement_. This ledger records exactly the part the code cannot.

ADRs remain the record of **decisions** (`docs/adr/`). This ledger is their mutable companion: an ADR says what was decided, a ledger row says how far that decision has actually got.

## Row schema

Every row carries **four mandatory fields**:

| Field                  | Meaning                                                                                                                      | Answers                                     |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| **Intended end state** | The shape the codebase is meant to reach when this change is finished. Written as an observable condition, not an intention. | _"What does done look like?"_               |
| **Current stage**      | Where the change actually is now, from the stages named in the row's own plan.                                               | _"How far did we get?"_                     |
| **Owner**              | A **named person** — never a role, a team, or "unassigned".                                                                  | _"Who decides the next step?"_              |
| **Review-by**          | A date or a named sprint by which the row is revisited.                                                                      | _"When does the silence become a problem?"_ |

> **A row missing any of these four fields is invalid.** It is not a draft, not a placeholder, and not "to be filled in later" — an incomplete row recreates precisely the untracked state this ledger exists to eliminate, while giving the false impression that the work is tracked. Either the four fields can be stated, or the change is not ready to land in stages.

Two optional fields may be added when they apply: **Gate** (the open question that must be answered before the next stage — e.g. `Q-5`), and **Closes** (what the row's completion allows to be deleted or retired).

## The entry-creation obligation

> **Landing a staged change adds its ledger row in the same change.** Not in a follow-up commit, not in the next sprint.

A row added later is written from memory, after the context is gone — which is how the original problem arose. If a pull request leaves the codebase in a state that is not the intended end state, that pull request carries the row explaining it.

The same obligation applies in reverse: **reaching the intended end state closes the row** in the change that reaches it. Rows are closed by moving them to [Closed rows](#closed-rows) with the date and the commit that finished them — never by silent deletion, because the record of _how long a stage took_ is itself worth keeping.

Forward-provisioning — shipping a capability before it has a consumer — is a staged change and needs a row (coding standard CS-3).

## Worked example

The following is an **illustration of the format only**. It is not a tracked row and describes no real work in this repository.

| ID        | Change                                                 | Intended end state                                                                                 | Current stage                                                                                              | Owner    | Review-by | Gate                                                         |
| --------- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | -------- | --------- | ------------------------------------------------------------ |
| `LDG-000` | _(example)_ Replace the legacy `foo` reader with `bar` | No call site reads `foo`; the `foo` module is deleted and its Firestore field is no longer written | Dual-read shipped: `bar` is authoritative, `foo` retained as fallback for documents written before 2026-07 | Jane Doe | Sprint 9  | `Q-00` — do pre-2026-07 documents still exist in production? |

Read that row as a sentence: _the end state is one reader; we are at two; Jane decides; if nothing has changed by Sprint 9 someone asks why; and the thing preventing the last step is a fact about production data nobody has checked._ A row that cannot be read that way is not finished.

## Rows

14 open rows (`LDG-02`, `LDG-06`, `LDG-10`, `LDG-11`, `LDG-12`, `LDG-14`, `LDG-17`, `LDG-19` closed below), grouped by the decision that governs them. Owner for every row is **NgnPhamGiaHuy**, who holds all three answering roles (product intent, GCP/ops console, hosting) — recorded in the Sprint 0 completion record. Review-by follows the wave in which each gate first bites: Wave 1 → Sprint 5, Wave 3 → Sprint 9, Wave 4 → Sprint 14, Wave 5 → Sprint 19, Wave 6 → Sprint 26.

Every stage claim below was verified against the working tree at `f3951e8`, not inherited from the planning documents — a row that misstates its stage is worse than no row at all.

### Notification migration (ADR-108)

| ID       | Change                           | Intended end state                                                                                                               | Current stage                                                                                                                                                                                  | Owner         | Review-by | Gate           |
| -------- | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------- | -------------- |
| `LDG-01` | Retire the legacy document shape | No document is read through `isUnread()`'s legacy branch; the four `@deprecated` fields are deleted; the legacy index is dropped | **Dual-read shipped and frozen, re-verified (T-108b/c/d, Wave 5); nothing removed.** `deckId`, `deckTitle`, `link`, `read` all live (`features/notifications/types/index.ts:81-88`); `isUnread()` still branches on the legacy shape (`:114-119`); the dual read-path/index machinery T-108d would collapse (`notification-subscribe.ts`'s primary/fallback listener swap, both composite indexes in `firestore.indexes.json`) and `scripts/backfill-notifications.mjs` are all confirmed still fully in place. Sharpened finding: this repo has **no `.firebaserc`** and `firebase.json` declares no project alias — so `NQ-1` isn't merely unanswered, it's unanswerable at the repo level; the local Firebase CLI's authenticated project cannot be assumed to be production. **Update 2026-07-22:** `NQ-1`'s index half is now partially answered empirically — `firebase firestore:indexes` confirms all 3 notification composite indexes (`status+isDeleted`, `read+isDeleted`, `isDeleted+createdAt`, plus a `status+createdAt` collection-group index) are live on `ngnphamgiahuy-devfolio`, deployed as a side effect of `firebase deploy --only firestore:indexes` run for the unrelated T-114a index (see closed `LDG-19`). This does **not** close `NQ-1`: `docs/testing-notifications.md`'s companion `firebase deploy --only firestore:rules` (Stage 2 rules) was not run this session and its live-deployment status is unverified, and formal production-project identity still has no `.firebaserc` record — only the strong circumstantial signal that `ngnphamgiahuy-devfolio` is the CLI's one and only linked project, and that this deploy visibly fixed a bug the owner's own authenticated session was hitting live. `Q-5` (production data shape) is untouched by any of this. **Narrowing note, 2026-07-22 cleanup audit:** the four `@deprecated` fields are not equally alive — `deckId`/`deckTitle` have **zero readers anywhere in-app** (`grep -rn '\.deckId\b\|\.deckTitle\b' features/notifications app` → 0 hits outside the write path), only `link` (1 reader, `domain/format.ts:56`, behind `data?.shareLink ??`) and `read` still have live readers. Also: the legacy corpus is still **growing**, not just inherited — `notification-pending.ts:118-119` (`notifyInvite`) writes `deckId`/`deckTitle` onto **new** docs today. Stopping those two writes is safe independently of this gate (zero readers, `data.shareLink` already covers `link`'s purpose) and would freeze the corpus at its current size while `Q-5`/`NQ-1` remain open — flagged for the gate owner, not self-invoked here. **Update 2026-08-04:** the flagged write stopped — `notifyInvite` (`notification-pending.ts`) no longer writes `deckId`/`deckTitle` onto new pending-invite documents, only `link` (matching its one live reader). No existing document was touched, and the `@deprecated` field declarations, the dual read-path, and both composite indexes remain exactly as before — T-108c/d's scope is unchanged. The legacy corpus is now frozen at its 2026-07-22 size going forward. **`Q-5` empirically sharpened the same day** (read-only `collectionGroup("notifications")` scan against `ngnphamgiahuy-devfolio`, no writes): of 50 documents, **12 (24%) are missing `status`** and are read only through `isUnread()`'s legacy `read`-boolean branch — real, current confirmation that the dual read-path is genuinely load-bearing today, not a residual risk. This does not answer `Q-5`/`NQ-1` (still no confirmation this project is production, and the rules-deploy half of `NQ-1` is still unverified) but it does confirm the harm scenario ADR-108 warns about is real for this data: collapsing the dual read-path now would misclassify these 12 documents' read state for real accounts. `Q-5`/`NQ-1` remain open and continue to gate everything else in this row | NgnPhamGiaHuy | Sprint 19 | `Q-5` + `NQ-1` |

`LDG-01` is the template row ADR-120 calls for: it names its gate, and its stage is a fact about the code rather than an intention. Its two gates are distinct — `Q-5` asks what shape production data is actually in, `NQ-1` asks whether the supporting index/rules deploy the runbook flags as _"NOT yet deployed"_ has since happened. Neither is answerable from the repository.

`LDG-02` (reconcile the type vocabulary) closed below — T-108a reached its full stated end state.

### Flashcard legacy-document compat (found during the 2026-07-22 cleanup audit)

| ID       | Change                                | Intended end state                                                                                                                       | Current stage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Owner         | Review-by | Gate  |
| -------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- | --------- | ----- |
| `LDG-22` | Retire lesson/card legacy-doc compat  | Every lesson doc carries `ownerId` and `roles` (no `userId`/`collaborators` fallback needed); every card carries fractional-index `order` (no numeric `order`/`sortOrder` reads); every card carries `alternatives` (no lazy heal-on-read) | **Split 2026-08-04 — empirically verified against the live database (`ngnphamgiahuy-devfolio`, read-only `collectionGroup` scan, no writes) rather than assumed.** **Lesson-level compat (4 of the original 6 mechanisms): REMOVED.** The scan found **0 of 9** lesson docs needed `userId`/`collaborators` fallback — every doc already carried `ownerId` and `roles`. Removed: `rbac.ts`'s `ownerId ?? userId` owner check; `lesson-subscriptions.ts`'s `collaborators array-contains` fallback query (and the matching `firestore.rules` collection-group branch, `resource.data.collaborators.hasAny(...)` — verified structurally redundant, not just empirically: every write site sets `roles` and `collaborators` atomically together, so no document could ever satisfy one without the other); the `collaborators` dual-write in `lesson-save.ts`/`lesson.service.ts`/`access.service.ts`; `lesson-normalize.ts`'s `__ownerIdFallback` and `sharedBy*`→`lastSharedBy*` mapping; `useLessons.ts`'s `userId: ownerId` write. `Lesson.userId`/`Lesson.collaborators` stay declared in the type as `@deprecated` (pre-2026-08-04 docs only) — nothing reads or writes them anymore. Full validation: typecheck clean, lint 0 errors, 375 unit + 84 browser + 133 emu (3 skipped, pre-existing) all green, including the real rules-suite exercising the new `firestore.rules` branch. **Card-level compat (2 of 6): CONFIRMED STILL LIVE, untouched.** The same scan found **192 of 817** cards (23.5%) missing `alternatives` — `card.service.ts`'s `assertCardSchema` heal-on-read stays load-bearing, not optional. **621 of 817** cards (76%!) still carry legacy numeric `order`/`sortOrder` — `shared/utils/reorder.ts`'s legacy-numeric compare stays load-bearing for the large majority of cards, not a long tail. Do not remove either without a backfill first — see the gate below, now narrowed to exactly these two mechanisms | NgnPhamGiaHuy | Sprint 26 | None — owner decision, narrowed to the card layer only: run a cards-only backfill (stamps fractional-index `order` + `alternatives: []` onto every existing card, mirroring `scripts/backfill-notifications.mjs`'s pattern), or accept the card-level compat cluster as permanent. The lesson-level half of this decision is now moot — already removed |

### Schema enforcement (ADR-109)

| ID       | Change                          | Intended end state                                                        | Current stage                                                                                                             | Owner         | Review-by | Gate   |
| -------- | ------------------------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------- | --------- | ------ |
| `LDG-03` | `cardContentSchema` disposition | Enforced at the card write boundary, or deleted — not declared and unused | **Declared, zero non-test consumers** (verified by grep); its header claims it is "the single validation source of truth" | NgnPhamGiaHuy | Sprint 14 | `Q-12` |
| `LDG-04` | `privacyModeSchema` disposition | Enforced at its write boundary, or deleted                                | **Declared, zero non-test consumers** (verified by grep). 2026-07-22: declaring file moved `shared/schemas/lesson.schema.ts` → `features/flashcard/types/lesson.schema.ts` (cleanup-audit A7) — disposition itself unchanged | NgnPhamGiaHuy | Sprint 14 | `Q-12` |
| `LDG-05` | `publicRoleSchema` disposition  | Enforced at its write boundary, or deleted                                | **Declared, zero consumers of any kind** — not even its own test file references it (verified by grep; the only zero-consumer schema of the three with no test coverage either). 2026-07-22: declaring file moved `shared/schemas/lesson.schema.ts` → `features/flashcard/types/lesson.schema.ts` (cleanup-audit A7) — disposition itself unchanged | NgnPhamGiaHuy | Sprint 14 | `Q-12` |

`LDG-06` (`Drawer` disposition) closed below — T-110b applied NQ-3's standing default.

### Data-layer guardrails: bounded queries and honest UI (ADR-114)

| ID       | Change                                        | Intended end state                                                                                                       | Current stage                                                                                                                                                                | Owner         | Review-by | Gate  |
| -------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------- | ----- |
| `LDG-07` | `analytics_daily` + `metadata/counters` reads | Both collections have a real writer, or their read paths are removed and every surface renders absent data **as absent** | **Honest-UI legs shipped (T-114b/c); read-path disposition still open.** The dashboard (`SystemHealthCard`, the 2 affected `AdminStatCard`s, `GrowthChart`/`ErrorTrendChart`) and the CSV export (`exportAnalyticsAction`) now render/emit an explicit absent-data marker instead of a fabricated 0 — re-verified by grep, 0 remaining read sites outside `user.service.ts`/`analytics.service.ts`/`admin.actions.ts`. The reads themselves are untouched: still written by nothing in-repo (functions/scripts included), so the collections' disposition — real writer vs. deleted — remains exactly as open as before. T-114d's own fallback ("delete the dead reads if Q-9 is unanswered") was deliberately **not** self-invoked — severing a possibly-live external contract is a hard-to-reverse call for the owner, not an agent, to make | NgnPhamGiaHuy | Sprint 9  | `Q-9` |

### Configuration and hosting (ADR-118)

| ID       | Change                               | Intended end state                                                      | Current stage                                                                                                                                                                                                                                                 | Owner         | Review-by | Gate  |
| -------- | ------------------------------------ | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------- | ----- |
| `LDG-08` | Canonical deployment origin          | A production origin is recorded as an ADR and `SITE_URL` resolves to it | **OPEN — no decision exists.** `SITE_URL` falls back to `http://localhost:3000` (`src/lib/site.ts:5`), and that fallback silently feeds sitemap, robots, `metadataBase`, OG images and every share URL. **Standing hazard: nothing errors when it is wrong.** | NgnPhamGiaHuy | Sprint 5  | `Q-2` |
| `LDG-09` | One `APP_ID` derivation (**T-118b**) | One derivation consumed by both the app and the functions package       | **BLOCKED, not started.** Satisfying it _is_ a tenant repartition if the two production env vars disagree: reads and writes move to a different `artifacts/{APP_ID}` root, nothing errors, and no code revert reunites the data                               | NgnPhamGiaHuy | Sprint 5  | `Q-6` |

### Dead surfaces (ADR-119)

| ID       | Change                                                     | Intended end state                                                                                            | Current stage                                                                                                                       | Owner         | Review-by | Gate   |
| -------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------- | ------ |
| `LDG-13` | `fanOutNotifications` callable                             | Wired into a request path, or deleted from the functions package                                              | **Deployed-but-untriggered by its own docblock, re-verified (T-119d, Wave 5); nothing deleted.** Still zero `httpsCallable` usage anywhere; the only `getFunctions` call is `fanout.ts` itself enqueueing via the Admin SDK, not a client invocation. `deliverNotificationTask`'s queue name matches `fanOutNotifications`'s enqueue target exactly, so by code construction these are one queue, not two independently-assessable bindings — narrowing Q-6 to one fact (has this ever been deployed and invoked) that remains outside repo-only reach. Fallback's own precondition (deployment facts confirmed first) stays unmet, so the default does not yet apply | NgnPhamGiaHuy | Sprint 19 | `Q-6`  |

`LDG-10` (7 dormant `NotificationKind`s), `LDG-11` (8 dormant `ActivityAction`s + `cloud_function` `LogSource`), `LDG-12` (inert admin controls) and `LDG-14` (Storybook toolchain) closed below — each Q-8/Q-11/Q-13/Q-17 gate's own standing fallback (delete) was applied, per ADR-119's "an undecidable gate resolves to the default."

### Admin authority (ADR-115)

| ID       | Change                           | Intended end state                                                                   | Current stage                                                                                                                                                              | Owner         | Review-by | Gate   |
| -------- | -------------------------------- | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------- | ------ |
| `LDG-15` | Align admin-authority predicates | One predicate answers "is this user an admin", consulted by app, rules and functions | **Three divergent predicates, re-verified (T-115c); no predicate touched.** `resolveCallerContext` (`features/admin/services/admin.service.ts:38`) grants on custom claim (`token.superadmin`/`token.admin`) **or** the Firestore `admins/{uid}.role` field's value. `isSystemAdmin()` (`firestore.rules:16-22`) grants on the same claims **or** mere *existence* of `admins/{uid}` — it never reads `.role`, so a doc with a garbage role value still passes rules but is denied by the app. `isAdmin()` (`functions/src/fanout.ts:120-124`) checks **only** `admins/{uid}.role` — it never reads custom claims at all, so a claims-only admin with no Firestore doc passes the app and rules checks but is denied by functions. Confirmed empirically: `setCustomUserClaims` is called nowhere in production code (grep across app/functions/scripts) — only in a test harness — so whether the claims branch is reachable in production at all is exactly Q-10. R-8 (admin bootstrap out-of-band) stays open for the same reason: how the *first* superadmin is created is undocumented and unverifiable from the repo. Per the task's own fallback, no predicate was changed — the standing default on this gate is inaction | NgnPhamGiaHuy | Sprint 14 | `Q-10` |

### One action client (ADR-106)

| ID       | Change                                        | Intended end state                                                                                                       | Current stage                                                                                                                                                                | Owner         | Review-by | Gate  |
| -------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------- | ----- |
| `LDG-20` | A confirmed third, unconverted "use server" pattern | "No third family — route handlers or ad-hoc transports — may be added" (ADR-106's own decision text); every server mutation runs through the unified client | **7 hand-rolled files bypass both the pre-unification and the new unified client entirely**, found during T-106a's audit: `features/flashcard/actions/activity-log.actions.ts` (7 fns), `features/kana/actions/activity-log.actions.ts` (2 fns), `features/flashcard/actions/access.actions.ts` (`declineInviteAction`, calls `adminAuth.verifyIdToken` directly, bypassing even the shared `verifyIdToken` helper), `features/user/actions/session.actions.ts` (this wave's own T-107a deliverable), `features/user/services/auth-logging.service.ts`, `lib/logging/actions.ts`, `lib/logging/user-actions.ts` (the actual shared verification core underneath the other 6). ADR-106's own "B vs C" framing and T-106a/b/c's task text only name the two originally-known clients — neither explicitly scopes this third pattern in or out. Not migrated here: expanding scope to 7 more files (including security-relevant ones) without being asked risks a much larger, unreviewed change than this wave's task breakdown anticipated | NgnPhamGiaHuy | Sprint 9  | None — owner decision: fold into T-106b/c's scope, or accept as a documented, intentionally-out-of-scope exception to "no third family" |
| `LDG-21` | M-2's "`toActionResult` retires with its last pre-migration caller" assumption | Either the shim is deleted with zero callers (M-2's original claim, from the retired refactor-program backlog), or the assumption is formally retracted and the shim is recognized as a permanent envelope adapter, independent of which safe-action client backs an action | **Assumption falsified by T-106d.** `adminActionClient` and `actionClient` are both deleted (zero callers, confirmed by repo-wide grep) — the client-unification end state genuinely is reached. But `toActionResult` (`lib/safe-action.ts`) is NOT gone: all 19 `admin.actions.ts` actions, fully migrated onto the unified `verifiedAdminActionClient` (T-106b), still call it. Its need was never actually tied to which client verifies identity — it exists so admin hooks/components can keep consuming `Promise<{ok,data}\|{ok,error}>` instead of next-safe-action's native `{data,serverError,validationErrors}` shape, and that consumer need doesn't disappear just because the *server-side* client got unified. M-2 assumed otherwise and was wrong. Not "fixed" here: inlining the same 3-line branch at 19 call sites to avoid calling a function literally named `toActionResult` would be pure churn with no behavior change — worse than keeping one named, tested adapter — and deleting it outright would break `admin.actions.ts`. The one real, still-open decision: whether admin hooks (and future actions) should eventually migrate off the `{ok,error}` envelope onto next-safe-action's own client-side conventions (e.g. `useAction`) — an unscoped initiative well beyond ADR-106's client-unification scope, not attempted here | NgnPhamGiaHuy | Sprint 14 | None — owner decision: keep `toActionResult` as a permanent utility (current default), or scope a separate initiative to migrate consuming hooks off the `{ok,error}` envelope entirely |

### Observability activation (ADR-116)

| ID       | Change                       | Intended end state                                                                                                             | Current stage                                                                                                                                                                                                          | Owner         | Review-by | Gate  |
| -------- | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- | --------- | ----- |
| `LDG-18` | Sentry/PostHog activation (T-116b/T-116c) | Either live with confirmed production credentials and project ownership, or explicitly retired — never left ambiguously wired | **Report-then-handle (T-116a) landed and is unconditional**: below-boundary failures on real state (SRS writes, Storage cleanup, login logging, invite delivery, admin cleanup) now report into the in-repo `system_logs` pipeline via `enqueueClientLog`/`persistSystemLog`, regardless of this gate. Sentry/PostHog activation itself is **not started** — no production DSN/API-key ownership has been confirmed in-repo (ADR-116 explicitly rejects deleting the existing wiring while this is unknown) | NgnPhamGiaHuy | Sprint 5  | `Q-4` |

### Landed in stages without a row (backfilled)

| ID       | Change                           | Intended end state                                                       | Current stage                                                                                                           | Owner         | Review-by | Gate                                                       |
| -------- | -------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- | ------------- | --------- | ---------------------------------------------------------- |
| `LDG-16` | Lint ratchet baseline (Sprint 0) | The baseline list is empty and every error-level rule applies everywhere | **12 files pinned to `warn`** in `src/eslint.config.mjs` so CI lint could become blocking without fixing unrelated code | NgnPhamGiaHuy | Sprint 14 | None — owner-driven; entries retire with T-116a and T-109a |

`LDG-16` is this ledger's own first test case. It was landed in Sprint 0, one commit before the ledger existed, and would otherwise have become exactly the kind of untracked staged state the ledger exists to prevent.

`LDG-17` (route cross-feature imports through barrels, T-101b/c) closed below — its full stated end state is now reached.

## Code markers cross-reference

Every deprecation marker and "reconcile later" comment in the tree maps to a row that names its removal condition. This list is the check ADR-120 asks for; if a new marker appears without a row, the check fails.

| Marker                                               | Location                                            | Row      |
| ---------------------------------------------------- | --------------------------------------------------- | -------- |
| `@deprecated deckId` · `deckTitle` · `link` · `read` | `features/notifications/types/index.ts:81,83,85,88` | `LDG-01` |
| `isUnread()` legacy fallback branch                  | `features/notifications/types/index.ts:114-119`     | `LDG-01` |

## Closed rows

| ID       | Change                                                 | Closed     | Commit   | Notes                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------- | ------------------------------------------------------ | ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `LDG-17` | Route cross-feature imports through barrels (T-101b/c) | 2026-07-21 | (T-101c) | End state reached in two stages: T-101b's retry (152 statements + 4 inline refs across 89 files) reduced deep cross-boundary imports to zero; T-101c added `import/no-restricted-paths` (9 feature zones + 1 app zone, uniform exception list of every feature's `index.ts`/`server.ts`) as `error`, verified to both fire on injected violations and pass the entire existing codebase with 0 errors. |
| `LDG-06` | `Drawer` disposition (T-110b) | 2026-07-22 | (T-110b) | NQ-3's standing default applied: `Drawer.tsx` deleted (re-verified zero render sites — both prior `<Drawer` matches were inside its own definition, a type annotation and a doc-comment example) along with its barrel export (`shared/components/ui/index.ts`). No owner veto was raised during the recorded veto window. The then-current shared-UI inventory was updated to stop advertising it. |
| `LDG-10` | 7 dormant `NotificationKind`s (T-119a) | 2026-07-22 | (T-119a) | Q-8's standing fallback applied: all 7 zero-producer kinds (`invite_declined`, `deck_updated`, `deck_deleted`, `privacy_changed`, `overtaken`, `leaderboard_top3`, `achievement`) deleted from the union and the registry, plus 2 now-dead `NotificationInput` fields (`gameMode`, `achievementKind`) whose only consumers were 3 of the deleted kinds. The emit schema and rendering carried zero kind-specific code for any of the 7 — a smaller real footprint than originally audited. |
| `LDG-02` | Reconcile the type vocabulary (T-108a) | 2026-07-22 | (T-108a) | `NotificationType` is now `NotificationKind \| "digest"` — a self-maintaining alias, not a hand-listed literal union — verified via `check-vocabulary-agreement.mjs`'s `NotificationType` target flipped from `report-only` to `enforce` and passing (`agrees (10 values)`). Sequenced after T-119a (shrinking `NotificationKind` to its 9 active members first) so the alias needed no exclusion list. `NotificationIcon.tsx` made genuinely exhaustive (an `assertNever` fallback, not a silent `default`), satisfying ADR-108's own success criterion that a non-exhaustive switch must fail typecheck. |
| `LDG-11` | 8 dormant `ActivityAction`s + `cloud_function` `LogSource` (T-119b) | 2026-07-22 | (T-119b) | Q-11's standing fallback applied: all 8 zero-producer `ActivityAction` members and `cloud_function` deleted. The kana-practice asymmetry resolved in the deletion direction (`KANA_PRACTICE_COMPLETED` removed, asymmetry recorded as intended — practice has no session/phase concept the way quiz/survival do). The one real follow-on fix: `lib/logging/public.ts`'s historical-value normalizer used a hardcoded 3-way literal check, not one derived from `LOG_SOURCES` — simplified to preserve the exact same "unknown → server" fallback behavior. No badge branch or filter dropdown existed to update; the original audit overstated that surface. |
| `LDG-12` | Inert admin controls (T-119c) | 2026-07-22 | (T-119c) | Q-13's standing fallback applied: `QuickActionsCard` (3 handler-less buttons) and its mount deleted; the Settings stub route, page, and its nav entries (`AdminSidebar` **and** `command-palette/data/actions.ts`, which mirrors it) deleted; `canChangeSettings` removed from `PermissionSet`/`ROLE_PERMISSIONS`, confirmed as its only 3 references repo-wide. The "action-metadata enum" the original audit also named to update turned out already deleted by this same wave's T-106d (ADR-106 unification) — one file (`rbac.ts`), not two. All orphaned i18n keys removed at both locales. |
| `LDG-14` | Storybook toolchain (T-119e) | 2026-07-22 | (T-119e) | Q-17's standing fallback applied (Low answerability — "nobody decided" is a live possibility): the 8 devDependencies, 2 scripts, `.storybook/`, the one story file, `eslint-plugin-storybook`'s wiring, and the 5 unreferenced scaffold SVGs are all removed. `Badge.tsx` itself (real, ~20 consumers) is untouched — only its story left. `vitest.config.ts`'s second project (`@storybook/addon-vitest` running the story as a Vitest browser test) removed along with its now-dead imports; the real unit-test project is verbatim. All non-Storybook lint rules (audio boundaries, `max-lines`, cross-feature/lib import boundaries) confirmed preserved. |
| `LDG-19` | Composite index deploy for the bounded public-lessons query (T-114a) | 2026-07-22 | (owner action, no code commit) | Owner ran `firebase deploy --only firestore:indexes` against `ngnphamgiahuy-devfolio` after confirming the `(isPublic ASC, createdAt DESC)` collection-group index was already correctly declared in `firestore.indexes.json`. Verified end-to-end in the live app: the Discover tab's `usePublicLessons` query failed with "requires an index" before the deploy, then "index is currently building" mid-build, then rendered its correct empty state ("No public decks yet") once the build finished — confirmed via a hard reload and a fresh console-error check. Side effect discovered during this same-day plan re-audit: the same deploy also pushed 3 notification composite indexes (`status+isDeleted`, `read+isDeleted`, `isDeleted+createdAt`) that `docs/testing-notifications.md` had separately flagged pending — now confirmed live via `firebase firestore:indexes`. Noted on `LDG-01` (still open) rather than closing it: this only answers the index half of `NQ-1`, not the rules half, and doesn't touch `Q-5` at all. |
